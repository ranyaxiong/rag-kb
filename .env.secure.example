# 安全配置示例 - 不包含明文API Key

# Application Configuration  
APP_NAME=RAG Knowledge Base
APP_VERSION=1.0.0
DEBUG=True

# Storage Configuration
UPLOAD_DIR=./data/uploads
CHROMA_DB_PATH=./data/chroma_db

# API Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_HOST=0.0.0.0
FRONTEND_PORT=8501

# RAG Configuration
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
MAX_SOURCES=3
SIMILARITY_THRESHOLD=0.7

# 安全的API Key配置方式 (选择其中一种)

# 方式1: 从文件读取 (推荐用于开发环境)
# OPENAI_API_KEY_FILE=./secrets/openai_api_key.txt

# 方式2: Base64编码的环境变量
# OPENAI_API_KEY_BASE64=your_base64_encoded_key_here

# 方式3: 系统环境变量 (最推荐)
# 在系统级别设置: export OPENAI_API_KEY="your-key"
# 无需在此文件中配置

# 注意: Docker Secrets会自动从 /run/secrets/openai_api_key 读取